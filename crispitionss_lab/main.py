import os
from dotenv import load_dotenv
from graph import lab_graph

# Cargar variables de entorno
load_dotenv()

def main():
    print("Iniciando Crispitionss Lab Agent...")
    
    # Aquí iría la lógica principal para ejecutar el grafo
    # Por ejemplo, cargar un dataset y llamar a lab_graph.invoke()
    
    print("Agente listo para recibir datos.")

if __name__ == "__main__":
    main()
